# TokenRemain 仓库工作约定

本文件适用于在此仓库工作的 Cursor、Codex 和子代理。它定义项目任务边界；宿主系统的更高优先级规则始终有效。用户在当前任务中的明确指令优先于这里的默认工作方式。通用技能、pstack playbook 和“继续直到完成”不能自行扩大任务授权。

## 开始任务

先读用户指定的计划、规格和文件，再检查 `git status --short`、当前分支和实际 diff。简要写明目标、允许修改的模块或文件、已有改动、验收条件，以及已经授予的提交、运行或发布权限。合理的局部实现选择直接推进，不为已授权的步骤重复询问。

| 范围 | 事实入口与边界 |
| --- | --- |
| macOS | `Package.swift`、`Sources/UsageDock/`、`Tests/UsageDockTests/`；SwiftPM、SwiftUI 与 AppKit |
| 共享同步 | `Packages/TokenRemainSyncKit/`、`Sources/UsageDock/Sync/`；有跨版本、跨设备消费者 |
| Windows | `windows/package.json`、`windows/README.md`、`.github/workflows/windows-validation.yml`；Electron 与原生 Windows 行为分开验证 |
| 官网 | `site/package.json`；构建检查与 Cloudflare 部署是不同操作 |
| Broadcast | `broadcast/package.json`、`broadcast/README.md`、`docs/curated-feed-contract.md`；本地测试不授权生产数据或消息操作 |
| 发布 | `docs/versioning.md`、`docs/download-baseline.md`、`script/`；版本以当前文件和 Git 状态为准，不照抄文档中的历史分支名 |

移动客户端源码独立维护。即使另一个仓库可访问，也不自动属于本任务范围。`marketing/`、`design/`、历史 `reports/`、`Vendor/`、版本文件、签名配置和发布产物只在任务相关时修改。

## 必须遵守的限制条件

### R1. 保留用户工作，只形成任务自己的 diff

- 已有 staged、unstaged、untracked 内容都不是可随意清理的临时文件；记录基线，结束时核对。
- 不为清空工作区执行 `git reset --hard`、`git clean -fd`、自动 stash 用户改动、批量 checkout/restore 或强制删除 worktree。不使用 `git add .` 混入无关内容。
- 需要隔离时创建独立 worktree，记录基准 commit。依赖未提交改动时，明确要带入的文件或 hunk，并保留原工作区；不能假设新 worktree 已包含它们。
- 回退失败实验只撤销本任务拥有的改动。发现范围外的问题，报告具体影响；范围内可独立完成的部分继续推进。

### R2. Git、发布和对外动作有明确授权边界

- 默认交付本地 diff 和验证结果；没有任务授权，不自动 commit、push、创建或更新 PR、merge、rebase 已有历史、打 tag。
- 不自动部署 Worker/网站、发布 Release/appcast、上传 App Store/TestFlight 或公证产物、修改生产 CloudKit schema/数据，也不向他人发消息或发布内容。
- 授权在当前任务内持续有效，执行前检查已有授权，不能反复要求确认。缺少必要授权时，先完成可独立进行的准备与验证，再呈现具体结果和待执行动作。
- “无人值守”“不要停”表示继续在现有范围内工作，不授予上述额外权限。发布标签不移动、不复用；真实发布按现有发布流程验证精确 commit 和产物。

### R3. 第三方凭证只读，后台采集不交互

- 自动采集只读取第三方工具自己维护的凭证；不主动刷新 token，不写回、迁移、删除或改权限来绕过认证失败。不自动登录或调用可能轮换凭证的认证命令。
- 读取经过 `Sources/UsageDock/Support/KeychainRead.swift` 等已有受控入口；后台使用非交互策略。用户主动授权入口必须与后台刷新隔离。
- 用户手动保存的 API Key、应用自己的同步密钥按现有专用路径处理；不要把“第三方凭证只读”误写成禁止应用自身所有 Keychain 写入。
- token、Cookie、认证文件、未脱敏响应和敏感日志不得进入源码、fixture、截图、审查上下文、会话历史摘要或外部服务。只提取定位所需的脱敏证据。

### R4. 保持同步隐私与跨版本兼容

- `MobileSnapshotRedactor` 是桌面 provider 模型进入同步 DTO 的明确边界；不直接序列化整个 Store、provider 模型或原始响应。
- 保留字段白名单、脱敏、加密认证、来源身份、序列号、过期及重放校验。`SyncContentFingerprint` 必须与可上传内容保持一致。
- 修改 wire schema、缓存、偏好或持久化格式前，识别已发布客户端和已有数据的消费者，提供旧数据与新数据验证及必要迁移设计。
- “迁移所有调用者后删除旧 API”仅适用于可以协调更新的内部 API。仓库中没有调用者不构成删除旧快照兼容路径的证据。

### R5. 行为合同不能被重构或性能优化偷偷改变

额度百分比/余额单位、独立额度池、账号归属、错误分类、缓存新鲜度、刷新间隔、退避与用户手动设置都是产品语义。结构重构先固定行为；改变这些语义必须属于任务目标。性能改动需比较同条件下的资源消耗和数据新鲜度，不以减少正常刷新或隐藏错误制造改善。

### R6. 清理必须局部且有证据

不在局部任务中全仓格式化、顺带统一架构、升级依赖或清理资产。许可证、安全与隐私约束、系统限制、协议兼容和历史故障原因注释应保留；确认信息过时或已由等价可执行约束覆盖后才可删除。不能因 `/no-comments`、`deslop` 或“更简洁”而删除约束或未经验证的兼容代码。

本地化和生成文件从当前权威来源维护，不硬编码语言数量。Windows 的 `src/locales.generated.js` 从 macOS `.strings` 生成，图标也有生成入口；先核对生成脚本和既有 diff，再生成必要产物，不手工修补生成结果或混入用户其他改动。

### R7. 并行必须隔离文件与运行状态

按宿主权限和任务需要使用子代理，小改动可单人完成。默认最多两个同时活动的执行子代理，不递归扩大数量；复杂设计或高风险变更才增加独立审查。每次委派传递本文件、允许修改范围、禁止操作、基线和验收条件，父代理复核实际 diff 与证据。

共享文件只有一个实现负责人。worktree 隔离不等于应用隔离：构建缓存、安装路径、进程名、端口、用户配置、Keychain、账号和 CloudKit 状态都可能共享。原生应用验证必须单一操作者；不得为自己的检查关闭别人的实例。

### R8. 按副作用选择验证，保护真实安装

执行入口见 [verify-tokenremain](.cursor/skills/verify-tokenremain/SKILL.md)。先读脚本实际行为，不凭 `verify`、`check`、`dry-run` 命名判断安全。

- 不默认运行 `script/verify_release_configuration.sh`，它会调用 ccusage `--update`。
- `script/build_and_run.sh --verify` 会停止对应进程、构建、安装并启动应用；普通 Dev 构建也需要实例归属检查。
- `--print-install-contract` 在当前脚本中也安装了 EXIT 清理，可能删除 `dist/` 下对应暂存 bundle；`verify_installation_isolation.sh` 调用它，不能当作无副作用入口。
- 普通开发保持非 sync 的 Dev 身份；不继承发布环境变量去覆盖生产 `TokenRemain.app`，不更改生产签名、权限或安装状态来让测试通过。

### R9. 测试与报告不能降低证据标准

修复优先用相关 fixture 或廉价失败用例复现；无需给纯文案和低影响规则编辑制造产品测试。先定向验证，再按影响范围扩大。共享核心或发布任务需要相应完整测试；新证据之外不反复重跑已通过检查。

不删除回归用例、放宽断言、篡改 fixture 或跳过失败检查来制造通过。历史环境失败只作排查线索，当前失败必须重新分类。分别报告：源码结论、定向测试、全量测试、真实运行结果、跳过/失败/未验证项。编译通过、进程存在、单张截图或子代理自述不能单独证明功能修复。审查结论引用精确 diff 或当前文件位置。

### R10. 长任务有预算和可续接记录

循环开始前写下完成条件、时间/调用/重试预算和停止条件。用户未给预算时，采用有界工作批次并记录判断依据，不创建无限循环。相同失败连续三次且没有新证据时停止该路径、保存证据并换可行路线；无法推进则报告阻塞，不放宽验收条件。决策记录只包含任务相关脱敏信息，保存到任务拥有的临时目录；是否入库单独依任务范围决定。

### R11. 规则维护与模型配置不暗中扩散

不因 `/reflect`、`/automate-me` 或技能故障自动扫描无关会话、改写个人记忆、全局模型映射、插件缓存或其他仓库规则。用户授权的规则改动独立成可审查 diff。模型只选当前环境可用者；缺少插件、模型或原生控制工具时明确降级方式，不伪造调用或验证结果。

### R12. 异步等待必须设计退出、呈现与恢复

涉及登录、授权、凭证验证、网络请求、子进程、同步或其他让用户等待的功能时，agent 在设计阶段必须按 [设计边界 DB-001](docs/design-boundaries.md#db-001) 判断是否需要超时，并记录理由、等待预算和退出条件；不能只设计成功路径，也不能将某次修复的秒数作为所有功能的默认值。

- 同时定义成功、失败、取消、超时和结果待确认时的界面：何时停止转圈、恢复哪些操作、保留哪些旧数据，以及用户下一步可以做什么。超时本身不等于凭证失效。
- 分清整次操作时限、阶段时限和无进展时限。主动结束的长任务也要有进度或存活检测、取消入口和生命周期边界；不设置总超时必须有明确依据。
- 取消/超时要传递到实际请求或子进程，清理自己拥有的资源；无法终止的底层操作必须隔离迟到结果并如实说明限制。阻塞等待不能占住 UI 线程。
- 在写凭证、发布状态或缓存前核对当前操作身份与取消状态。超时后不能继续悄悄保存，旧请求不能覆盖新请求；有副作用且结果未知的操作先核实结果，再决定是否可安全重试。
- 验收覆盖不返回、超时、取消、重试、迟到结果和部分完成，按风险补对应回归。模拟请求必须隔离实际写入路径，包括额度历史等间接缓存，不能只隔离 UI 或主缓存。

R10 约束 agent 的工作预算；本条约束产品内的等待行为，两者不能互相替代。

### R13. 常见内容直接展示，额外交互需要理由

涉及卡片、列表或信息面板时，按 [设计边界 DB-002](docs/design-boundaries.md#db-002) 先检查常见内容能否在合理尺寸内完整呈现。优先调整共享尺寸、间距和信息层级；不要为少量内容制造内层滚动、折叠、分页或额外操作。

- 用真实组成判断高度：标题、进度条、重置时间、配速、更新时间及必要提示都属于内容，不能只计算进度条数量。
- 不能靠裁切、隐藏状态、缩小到难读的字号或强制单行来假装装得下；也不能无依据地把所有卡片无限加高。
- 多账号、多额度池等确实超出的内容可保留滚动或渐进展示，但需要说明其必要性；内容装得下时不出现内层滚动条、不截获页面滚动。
- 验收覆盖最窄支持宽度、较长本地化文案、常见/溢出/动态状态和真实滚动操作；能用局部布局解决时，不引入新的交互状态机或布局框架。

## 标准交付

按“范围与基线 → 理解/复现 → 最小实现 → 对应验证 → 审查 diff → 交付”推进。最终说明具体行为变化、选择依据、验证结果及限制，列出实际修改的文件；只有存在已授权的下一阶段，才继续 Git 或发布操作。规则是代理约定，不是操作系统权限屏障；可执行契约和 CI 的结果仍是独立证据。
