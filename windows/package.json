{
  "name": "tokenremain-windows",
  "version": "1.2.6-windows.1",
  "private": true,
  "description": "TokenRemain for Windows with encrypted direct Mac sync",
  "main": "electron/main.js",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "start": "npm run build && electron .",
    "test": "node --test tests/*.test.js",
    "check": "node --check electron/main.js && node --check electron/preload.cjs && node --check electron/feed.js && node --check electron/popover-placement.js && npm test && npm run build",
    "dist:win": "npm run build && electron-builder --x64 --win nsis portable --publish never"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.7.0",
    "vite": "6.4.3",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "build": {
    "appId": "com.jamesli.tokenremain.windows",
    "productName": "TokenRemain",
    "asar": true,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../site/assets/brand/appicon-mac.png",
        "to": "appicon.png"
      }
    ],
    "win": {
      "icon": "../site/assets/brand/appicon-mac.png",
      "target": ["nsis", "portable"]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "deleteAppDataOnUninstall": false
    }
  }
}
